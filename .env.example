APP_ENV=development
APP_DEBUG=true

# Option 1: DATABASE_URL (set in .env only; do not commit real credentials)
# - Local / external: use Render "External Database URL" (full hostname)
# - Backend on Render: use Render "Internal Database URL" (short hostname)
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE

# Option 2: individual vars (Render – use .env for real password)
POSTGRES_HOST=dpg-d6p462s50q8c73c5n8vg-a.oregon-postgres.render.com
POSTGRES_PORT=5432
POSTGRES_USER=abubakkarsajid1
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=ai_property_analysis

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

LOG_LEVEL=INFO
LOG_JSON=false

# Rate limiting (e.g. 200/minute)
RATE_LIMIT_DEFAULT=200/minute

# Optional auth: set AUTH_ENABLED=true and API_KEY to require X-API-Key header
AUTH_ENABLED=false
API_KEY=

